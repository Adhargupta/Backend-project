middleware - codes that runs in between request and response 

-> example - for adding cookies(to check wether user can take those info or not) between the request send to server and server sends the response